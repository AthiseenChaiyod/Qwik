import { component$ } from "@builder.io/qwik";
import { UserParent } from "~/components/docs/components/context/context";

export default component$(() => {
  return (
    <>
      <UserParent />
    </>
  );
});
